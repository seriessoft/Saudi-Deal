var firebase = require('firebase;);


var evaluatePromotion = function(){
  if (winStreak => winPromo){
    if(wallet => promoFees){
      //updateSubRank
      //updateRank
    }
  }
};

var evaluateDemotion = function(){
  if(rank => 1)
    if(lossStreak => lossDemo){
      //updateSubRank
      //updateRank
    }
  }
};

